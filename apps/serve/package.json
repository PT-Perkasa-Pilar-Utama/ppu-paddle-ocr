{
  "name": "ppu-paddle-ocr-serve",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Production-grade REST API serving ppu-paddle-ocr (Hono + Bun).",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "type-check": "bunx tsgo --noEmit"
  },
  "comment": "Standalone app: ppu-paddle-ocr is imported from source via tsconfig paths (../../src), so its deps (ppu-ocv, onnxruntime) are declared here too.",
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "hono": "^4.6.14",
    "onnxruntime-common": "^1.26.0",
    "onnxruntime-node": "^1.23.2",
    "ppu-ocv": "^3.1.5",
    "zod": "^3.24.1"
  },
  "trustedDependencies": [
    "onnxruntime-node"
  ]
}
