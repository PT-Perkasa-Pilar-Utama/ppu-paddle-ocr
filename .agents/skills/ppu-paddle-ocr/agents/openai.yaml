interface:
  display_name: "Ppu Paddle Ocr"
  short_description: "Repo-specific patterns and workflows for ppu-paddle-ocr"
  default_prompt: "Use the ppu-paddle-ocr repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true